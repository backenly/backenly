# CLAUDE.md

See [AGENTS.md](AGENTS.md).

That file is the single source of truth for how agents should work in this
repository, and it is vendor-neutral. This pointer exists only so that tools
looking for `CLAUDE.md` by name still find the guidance.

Do not copy content back into this file. It was duplicated once before; the
copies drifted, and a security scrub applied to one of them left the other
still publishing a production server address.
